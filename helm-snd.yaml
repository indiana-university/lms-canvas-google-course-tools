
bootstrapSecret: "protected-properties-2020-07-10"

image:
  repository: registry-snd.docker.iu.edu/lms/google_course_tools

imagePullSecrets:
    - name: registry-sand-cred

env:
  - name: APP_ENV
    value: snd

activeProfiles: "redis"

ingress:
  enabled: true
  hosts:
    - host: "gct-snd.apps-snd.iu.edu"
      paths: []