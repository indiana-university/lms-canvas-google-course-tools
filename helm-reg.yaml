
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "gct-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis-session,reg,ehcache"

serviceAccountName: googlecoursetools-reg

cluster: es-test
